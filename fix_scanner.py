import codecs

with codecs.open("src/app/factory/scanner/page.tsx", "r", "utf-8") as f:
    content = f.read()

# 1. Update the stage logic
old_logic = "const nextStage = orderData.currentStage + 1;"
new_logic = "const nextStage = selectedStage;"
content = content.replace(old_logic, new_logic)

# 2. Update the error condition (no longer checking > 14 since selectedStage is always valid)
old_error = """      if (nextStage > 14) {
        throw new Error("?? ???? ???? ??????? 14 (??????)");
      }"""
new_error = ""
content = content.replace(old_error, new_error)

# 3. Update the success message
old_success = "?? ??? ??????? ????? ??? ??????? ???????"
new_success = "?? ????? ???? ??????? ????? ???"
content = content.replace(old_success, new_success)

# 4. Update the button text
old_button = "?? ????????"
new_button = "????? ?????? ??????"
content = content.replace(old_button, new_button)

# 5. Add a small text instruction adjustment
old_text = "??? ?????? ?? ????? ??????"
new_text = "????? ???? ??????? ??? ????"
content = content.replace(old_text, new_text)

with codecs.open("src/app/factory/scanner/page.tsx", "w", "utf-8-sig") as f:
    f.write(content)

print("Scanner fixed")
